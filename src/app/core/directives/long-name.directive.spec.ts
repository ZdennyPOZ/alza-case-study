import { LongNameDirective } from './long-name.directive';

describe('LongNameDirective', () => {
  it('should create an instance', () => {
    const directive = new LongNameDirective();
    expect(directive).toBeTruthy();
  });
});
